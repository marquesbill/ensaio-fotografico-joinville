node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.vercel
.env*.local

# vídeos grandes (não versionar)
teaser40s.mov
*.mov

# Galerias preparadas localmente (imagens + zips) — nunca vão para o repositório
staging/
